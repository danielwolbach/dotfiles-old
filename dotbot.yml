- defaults:
      link:
          create: true
          relink: true

- clean:
      "~/":
      "~/.config/":
          recursive: true

- link:
      ~/.config/:
          glob: true
          path: config/**
