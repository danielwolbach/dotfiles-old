- defaults:
      link:
          create: true
          relink: true
      clean:
          force: true

- clean:
      "~/":
      "~/.config/":
          recursive: true

- link:
      ~/.bashrc:
          path: home/bashrc
      ~/.config/:
          glob: true
          path: config/**
